# Feature Specification: AutoSCORE 2段階採点パイプライン (002-autoscore-pipeline)

> **ステータス**: Approved / Implemented  
> **対象レイヤー**: Feasibility（実稼働）  
> **アーキテクチャ・設計詳細**: [plan.md](plan.md)  
> **全体アーキテクチャ・ER図**: [specs/README.md](../README.md)  

---

## 1. 概要とユーザー価値 (What & Why)

### 1.1 解決する課題
LLMに受講者の生ログを直接読ませて点数をつけさせると、「自分がつけた点数を正当化するための理由」を事後的に捏造（ハルシネーション）し、測定の再現性や公平性が崩壊する。
本機能では、測定学の規律に基づき「事実の客観的抽出（第1段階）」と「抽出結果に基づく規準的バンド判定（第2段階）」を厳格に分離したパイプラインを提供する。

### 1.2 提供価値・ゴール
1. 採点の客観的根拠（発言引用・不備指摘・正常箇所誤認）が透明に可視化される（XAI）。
2. LLMの確信度が低い（< 0.7）境界ケースは自動で人間レビュー待ち（`pending_human`）に回す。
3. エラー時やAPI未設定時にキーワード一致等のフォールバック採点を絶対に走らせず、測定データの完全性を死守する。

---

## 2. ユーザーストーリーと受入基準 (Acceptance Criteria)

### [US1] Story 1: 第1段階 根拠要素の客観的抽出 (extractEvidence)
* **前提 (Given)**: 対話演習ログ、成果物エディタの内容、課題仕様が存在する。
* **操作 (When)**: 第1段階パーサーが実行される。
* **結果 (Then)**:
  * 受講者の発言から具体的な検証行動（仕込み不備の指摘、トレードオフの言語化、正常箇所への過剰指摘、無批判な受容）が `EvidenceComponent` スキーマ（Zod）で抽出される。
  * メディエーターの発言は抽出対象外（受講者の発言のみが根拠）とされる。

### [US2] Story 2: 第2段階 ルーブリック基準によるバンド評定 (computeBandScore)
* **前提 (Given)**: 第1段階で抽出された構造化根拠データ（生ログは渡さない）が存在する。
* **操作 (When)**: 第2段階バンド採点器が実行される。
* **結果 (Then)**:
  * 軸4（AI協働検証力）のルーブリック基準に基づき、0〜5のバンドスコアが算出される。
  * 採点の自己申告確信度（`scoring_confidence`: 0.0〜1.0）および判定論拠要約が返される。

### [US3] Story 3: 確信度閾値による HITL（Human-in-the-Loop）遷移
* **前提 (Given)**: 採点器の自己申告確信度が閾値 0.7 未満である場合。
* **操作 (When)**: スコア確定処理が実行される。
* **結果 (Then)**:
  * `rater_type = "pending_human"` として記録され、スコアは保留状態となる。
  * 確信度が 0.7 以上の場合は `rater_type = "llm"` として即座に確定する。

### [US4] Story 4: フォールバック採点の絶対禁止（Principle III）
* **前提 (Given)**: OpenAI API キーが未設定、ネットワークタイムアウト、または構造化出力パースに失敗した場合。
* **操作 (When)**: `/api/dialogue/evaluate` が呼び出される。
* **結果 (Then)**:
  * キーワード一致やローカル計算による代替スコア（フォールバック）を絶対に生成してはならない。
  * `ScoringUnavailableError` をスローし、HTTP 500 / 503 としてエラーレスポンスを返さなければならない。

### [US5] Story 5: 2段階データの完全永続化
* **前提 (Given)**: 採点パイプラインが正常終了する。
* **操作 (When)**: DB 保存処理が実行される。
* **結果 (Then)**:
  * 抽出されたすべての根拠が `EvidenceComponent` テーブルに保存される。
  * 確定した評定が `Rating` テーブルに保存され、両者がリレーションで結合される。

---

## 3. レイヤー区分とモック境界 (Boundary Definition)

* **実稼働（Feasibility）の範囲**:
  * `src/lib/evaluator/index.ts`（2段階パーサー、Zodスキーマ、プロンプト）。
  * `/api/dialogue/evaluate` ルートハンドラー。
  * Prisma `Rating`, `EvidenceComponent` のトランザクション保存。
* **モック（Viability）の範囲**:
  * 組織ダッシュボードの全体平均スコア集計等はモック表示とし、本パイプラインのリアルタイム算出負荷を与えない。

---

## 4. Grill（逆質問）記録と前提の解消

1. **Q: なぜ第2段階に生ログを渡してはいけないのか？**
   * **A**: 生ログを渡すとLLMが「受講者がたくさん喋っているから高得点」「口調が丁寧だから高得点」といった非本質的なバイアスに引っ張られ、第1段階の客観的根拠を無視したスコアリングを行ってしまうため（測定学的要求）。
2. **Q: OpenAI API が落ちたときに簡易採点で受講者を待たせない方がUXが良いのでは？**
   * **A**: 本アセスメントは測定ツールであり、フォールバック採点（単語マッチング等）は「検証行動をしていないのに単語が入っていただけで高得点」を生む致命的欠陥となる。測定の厳密性を損なうフォールバックは憲法 Principle III で明確に禁止されている。
